version: "3.7"

services:
  hub:
    image: werty1st/espruinowebide
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    privileged: true
    environment:
      - BLENO_HCI_DEVICE_ID=0
    volumes:
      - /dev:/dev
    network_mode: host
